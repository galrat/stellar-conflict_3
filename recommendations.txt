Надо добавить в state сразу во время его формирования следующие данные:

1. координаты каждой area внутри тайла это значение area_idx которое равно 0, 1, 2 или 3 где 0 - левая верхняя область 1 - правая верхняя область, 2 - нижняя левая область 3 - нижняя правая область, но параметр тайлов rotation оставляем
2. убрать из параметров тайла owner у тайла нет владельца владельцы есть только у областей
3. убрать из hand_battle_cards парметр level так как уже есть парметр tier
4. добавить в troops параметр ready_to_move со значением 1 и 0



Нужно изменить game_orders.js JS не должен ничего проверять не должен проверять тип приказа, он только сообщает какой приказ был выбран и куда поставлен, эти данные он отправляет в сервер а том создает новый state, но пока сохраняет и старый state, если игрок передал ход, то новый state заменяет старый state, если игрок нажал вернуть приказ в руку, то сервер удаляет новый state и ждет когда пользователь выставит приказ и нажмет кнопку передать ход. именно сервер проверяет выставлен ли один приказ и если приказ не выставлен то именно сервер формирует сообщение об этом а JS только визуализирует его. Также сервер проверяет что все приказы выставлены всеми игроками и инициирует этап розыгрыша приказов



касательно приказа deploy

«Перепиши game_deploy.js так, чтобы он не содержал никакой логики цен и условий. Все данные для кнопок (текст, цена, доступность) должны браться из G.pending_deploy.deploy_info. При клике на карту JS должен передавать только area_idx, полностью полагаясь на валидацию сервера.»
Задача: Оптимизировать механику приказа Deploy, перенеся всю игровую логику из JavaScript на Python (Backend). Сделай фронтенд «тонким» — он должен только отображать данные, присланные сервером.

Контекст системы:

deploy.py: Основной файл логики приказа.

game_deploy.js: Файл интерфейса.

chaos.py: Конфиг фракции (используй структуру UnitConfig для типов юнитов).

test.json: Текущее состояние игры (карта, игроки, здания).
касается приказа deploy

Что нужно сделать (Backend - Python):

Реализуй функцию get_deploy_info(state, player_idx, system_key), которая:

Считает общее количество городов игрока по всей карте в state['map'].

На основе городов и наличия жетонов forge определяет доступный Тир юнитов.

Формирует список unit_catalog, беря типы юнитов из конфига фракции (напр. CHAOS). Для каждого юнита укажи: key, name, cost, tier, available (boolean) и reason (почему недоступен).

Рассчитывает capacity (лимит постройки) для указанной системы, суммируя вместимость областей, где у игрока есть factory.

Обнови функцию deploy_order, чтобы она записывала результат get_deploy_info в state['pending_deploy']['deploy_info'].

Что нужно сделать (Frontend - JS):

Полностью удали из JS константы цен и условий (_DEPLOY_COSTS, расчеты Тиров).

Перепиши _renderDeployBuyUnitsModal так, чтобы она:

Использовала данные только из G.pending_deploy.deploy_info.

Итерировала по присланному массиву unit_catalog и создавала кнопки покупки.

Блокировала кнопки (disabled) только на основе флага available из присланных данных.

Выводила reason в интерфейс, если юнит недоступен.


по поводу game_serializer.py

В функции generate_game_state, там где идет перебор областей (for area in tile.areas), обязательно убедись, что ты добавляешь тот самый idx, о котором мы говорили:
Нумерация областей (idx):
это критически важно. При сборке словаря карты он должен явно добавлять индекс каждой области.
Зачем: Чтобы game_render_board.js мог записать это в dataset.areaIdx, а game_deploy.js мог отправить этот индекс обратно при клике.
При формировании состояния для активного приказа Deploy, сериализатор должен включать объект deploy_info, данные для которого (цены, доступность юнитов) берутся из Python-файлов фракций (например, chaos.py).
Убедись, что все временные данные (pending_deploy, pending_advance) корректно включаются в JSON, чтобы фронтенд мог мгновенно обновить интерфейс после apiCall.


касательно game_state.py
«Обнови game_state.py так, чтобы объект GameState содержал поле pending_deploy. При сериализации в to_dict это поле должно включать всю необходимую информацию для фронтенда (каталог юнитов, цены, доступные области), чтобы JS-код не делал никаких расчетов самостоятельно. Убедись, что area_index в методах расстановки интерпретируется как физический индекс в массиве areas.»

